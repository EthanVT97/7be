const config = {
    API_BASE_URL: 'https://twod3d-lottery-api.onrender.com',
    ENDPOINTS: {
        AUTH: {
            LOGIN: '/auth/login',
            REGISTER: '/auth/register'
        }
    }
}; 